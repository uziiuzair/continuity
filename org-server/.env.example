# Required — the bearer token that plugins authenticate with.
# Generate a strong random string, e.g.:
#   openssl rand -base64 48
CONTINUITY_ORG_API_KEY=change-me-to-a-long-random-string

# Optional — host port to expose the server on (defaults to 8787)
HOST_PORT=8787
